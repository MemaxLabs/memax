Chunking strategy for long documents: semantic chunking (按段落 + heading boundary)，不用 fixed-size sliding window。chunk size target 200-500 tokens，overlap 0。每个 chunk 保留 parent memory id 和 position index。

TLS and encryption decisions: all traffic TLS 1.3, Fly.io handles TLS termination. Database connection uses SSL mode=require. R2 objects encrypted at rest by default. 不需要额外的 application-level encryption for now。

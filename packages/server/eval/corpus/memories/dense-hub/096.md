Considered using Cloudflare Workers for rate limiting at the edge but decided against it. Our auth tokens need to be validated server-side anyway, so moving rate limiting to the edge doesn't save a round trip. Keep it simple in the Go middleware.
